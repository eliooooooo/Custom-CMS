{{dump(blocks)}}
<div class="block {{blocks.name}}" >
  <h1>{{blocks.name}}</h1>
  <h2>{{article.subtitle}}</h2>
  {% for element in article.elements %}
    {% include "display/display_elements.html.twig" %}
  {% endfor %}
</div>
