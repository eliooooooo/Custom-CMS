{% extends "page.html.twig" %}

{% block title %}Admin - Update{% endblock %}

{% block content %}

    <h1>Update</h1>

    {# Affichage des éléments #}
    <details>
        <summary>Element</summary>
        {% for element in element %}
            <form method="POST" action="{{site_url}}element?action=update&id={{element.id}}" >
                <div class="group-form">
                    <label for="tags">Balise :</label>
                    <input type="text" id="tags" name="tags" placeholder="{{element.tags}}" required>
                </div>
                <div class="group-form">
                    <label for="content">Contenu :</label>
                    <input type="text" id="content" name="content" placeholder="{{element.content}}">
                </div>
                <div class="group-form">
                    <label for="class">Classes :</label>
                    <input type="text" id="class" name="class" placeholder="{{element.class}}">
                </div>
                <div class="group-form">
                    <label for="id_article">Article :</label>
                    <select name="id_article" id="id_article" required>
                        {% set id_article = element.id_article %}
                        <option value="1" {% if id_article == 1 %} selected {% endif %}>Article 1</option>
                        <option value="2" {% if id_article == 2 %} selected {% endif %}>Article 2</option>
                        <option value="3" {% if id_article == 3 %} selected {% endif %}>Article 3</option>
                    </select>
                </div>
                <div class="group-form-check">
                    <div class="group-form">
                        <label for="link-checkbox">Lien :</label>
                        <input type="checkbox" id="link-checkbox" name="link-checkbox" class="toggle-input" {% if element.link != "" %}checked{% endif %}>
                        <input type="text" id="link" name="link" placeholder="{{element.link}}" {% if element.link != "" %}{% else %}style="display: none;"{% endif %}>
                    </div>
                    <div class="group-form">
                        <label for="alt-checkbox">Alt :</label>
                        <input type="checkbox" id="alt-checkbox" name="alt-checkbox" class="toggle-input" {% if element.alt != "" %}checked{% endif %}>
                        <input type="text" id="alt" name="alt" placeholder="{{element.alt}}" {% if element.alt != "" %}{% else %}style="display: none;"{% endif %}>
                    </div>
                </div>
                <input type="submit" value="Créer">
            </form>
        {% endfor %}    
    </details>
    {# Affichage des articles #}
    <details>
        <summary>Article</summary>
        {% for article in article.articles %}
            <form method="POST" action="{{site_url}}article?action=update&id={{article.id}}">
            <div class="group-form">
                <label for="title">Titre :</label>
                <input type="text" id="title" name="title" placeholder="{{article.title}}" required>
            </div>
            <div class="group-form">
                <label for="subtitle">Sous-titre :</label>
                <input type="text" id="subtitle" name="subtitle" placeholder="{{article.subtitle}}">
            </div>
            <div class="group-form">
                <label for="author">Auteur :</label>
                <input type="text" id="author" name="author" placeholder="{{article.author}}">
            </div>
            <div class="group-form">
                <label for="class">Classe :</label>
                <input type="text" id="class" name="class" placeholder="{{article.class}}">
            </div>
            <div class="group-form">
                <label for="id_category">Category :</label>
                <select name="id_category" id="id_category" required>
                    {% set id_category = article.id_category %}
                    <option value="1" {% if id_category == 1 %} selected {% endif %}>Category 1</option>
                    <option value="2" {% if id_category == 2 %} selected {% endif %}>Category 2</option>
                    <option value="3" {% if id_category == 3 %} selected {% endif %}>Category 3</option>
                </select>
            </div>
            <input type="submit" value="Créer">
        </form>
        {% endfor %}    
    </details>
    {# Affichage des catégories #}
    <details>
        <summary>Category</summary>
        {% for category in category.category %}
        <form method="POST" action="{{site_url}}category?action=update&id={{category.id}}">
            <div class="group-form">
                <label for="name">Nom de la categorie :</label>
                <input type="text" id="name" name="name" placeholder="{{category.name}}" required>
            </div>
            <div class="group-form">
                <label for="description">Description :</label>
                <input type="textarea" id="description" name="description" placeholder="{{category.description}}">
            </div>
            <div class="group-form">
                <label for="image">Image :</label>
                <input type="text" id="image" name="image" placeholder="{{category.image}}">
            </div>
            <div class="group-form">
                <label for="class">Classe :</label>
                <input type="text" id="class" name="class" placeholder="{{category.class}}">
            </div>
            <input type="submit" value="Créer">
        </form>
        {% endfor %}    
    </details>

{% endblock %}
