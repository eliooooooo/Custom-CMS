/// <reference types="less" />
import { Plugin } from 'esbuild';
import * as postcss from 'postcss';
export interface Options {
    root?: string;
    lessOptions?: Less.Options;
    postcssPlugin?: postcss.AcceptedPlugin[];
    injectScripts?: (id: string, content: string) => string;
    /**
     * 是否生成 .d.ts 描述文件
     * @description 配置true时，请添加 ignore  *.less.d.ts 或者 *.css.d.ts
     * */
    declaration?: boolean;
}
export default function ({ root, lessOptions, postcssPlugin, injectScripts, declaration, }?: Options): Plugin;
